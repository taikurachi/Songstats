export default function Details({ dominantColor }: { dominantColor: string }) {
  return (
    <div
      className="p-8 w-full mr-2 rounded-lg"
      style={{
        background: dominantColor,
      }}
    >
      asdf
    </div>
  );
}
